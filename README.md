# ZSS
Combined Gsap with Three.js to build website with very complex visuals and animations , but sadly animations are so heavy that the website is useless as it causes too much lag or even browser to crash.
